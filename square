/ Online C compiler to run C program online
#include <stdio.h>

int main() {

int L,area;
printf("enter length of square L");
scanf ("%d",&L);
area=L*L;
    printf("area of sruare is %d",area);

    return 0;
}
